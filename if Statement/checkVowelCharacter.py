# Program to check if character is vowel

# variable to store user input
char = input('Enter character: ')

# displaying result to user
if char in ['a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U']:
    print(char, 'is vowel')
