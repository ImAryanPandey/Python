# Program to check if number is less than or equal to zero.

# variable to store user input
num = int(input('Enter number: '))

# displaying result to user
if num <= 0:
    print(num, 'is less than or equal to zero.')

